%section.confirm_password_reset.container
  .row
    .col-sm-10.col-sm-offset-1
      %p We have send an email with instruction to reset your password.
      - user = User.find_by(email: "juan@test.com")
      = link_to "Reset password", reset_password_path(user.token)